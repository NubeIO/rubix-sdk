/**
 * Export all types and utilities from generated client
 */
export * from './client';
export * from './types';

/**
 * Export utility functions
 * These are NOT generated - they are manually maintained helpers
 */
export * from './utils';

// Smart client removed - use rasClient directly, backend handles routing
